import './style.css';
import { 
    handleOperandClicks, 
    handleOperatorClicks, 
    handleUtilClicks,
    handleKeyPress
} from './handleEvents.js';

handleOperandClicks();
handleOperatorClicks();
handleUtilClicks();
handleKeyPress();